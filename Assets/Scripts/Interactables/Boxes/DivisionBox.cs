using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DivisionBox : MonoBehaviour
{
    /*[SerializeField] private GameObject playerPrefab;
    [SerializeField] private bool activate;
    private PlayerController prefabController;

    void Start(){
        prefabController = playerPrefab.GetComponent<PlayerController>();
    }


    private void OnTriggerEnter2D(Collider2D other){
        if(other.CompareTag("Player") || other.CompareTag("PlayerCopy")){
            if(activate){
                prefabController.canDivide = true;

                PlayerController aux = FindFirstObjectByType<PlayerController>();
                if(aux != null)
                    aux.canDivide = true;
            }
            else{
                prefabController.canDivide = false;

                PlayerController aux = FindFirstObjectByType<PlayerController>();
                if(aux != null)
                    aux.canDivide = false;
            }
        }
    }*/
}